@model IEnumerable<EjemploColegioMVC.Models.Calificacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Acumulado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Examen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estudiante.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Asignatura.Codigo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Acumulado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Examen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estudiante.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Asignatura.Codigo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
